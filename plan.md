# Potential changes

1. More "correct" member accessibility in piece, board, game
1. Game loop (something like: "game.play()")
    - printPrompt()
    - getUserMove() -> parseMove()
    - tryMove()
1. Add game logic for checkmate ++
1. Initialize pieces in board class?
1. Global formatting style
1. Build - system: CMakeLists.txt or makefile
1. constexpr shit that can be constexpressed
